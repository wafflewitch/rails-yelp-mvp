<h1>Write a New Review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, as: :select, collection: Review::RATING, include_blank: "Select a rating" %>
  <%= f.input :content %>
  <%= f.submit "Add a Review", class: "btn btn-pink" %>
<% end %>
